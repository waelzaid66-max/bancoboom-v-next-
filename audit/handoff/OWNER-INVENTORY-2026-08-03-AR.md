# الجرد — حالة الريبو الحقيقية

**التاريخ:** 2026-08-03 · **الدور:** أنا المالك (تكليف صريح) · **الفرع:** `main` = `29469d0`

> **قاعدة هذا المستند:** كل سطر تحته أمر تشغّله بنفسك. اللي مقدرتش أتحقق منه مكتوب
> إنه غير متحقق — مش مكتوب إنه تمام.

---

# 0) الحدث الأهم — الـCI اشتغل لأول مرة في تاريخ الريبو

كل مستند سابق في المجلد ده — **بما فيها بتاعي** — كان بيقول إن الأرقام الخضراء
«محلية وغير متحقق منها آليًا»، لأن `total_count: 0` على أربع workflows.

**النهاردة شغّلت الـCI يدويًا (`workflow_dispatch`) على `main`.**

| الحقيقة | القيمة |
|---|---|
| الـworkflows | 4 · كلهم `state: active` منذ 2026-08-01 |
| التشغيلات قبل النهاردة | **صفر** |
| التشغيل الأول | **run #1** · `30841315812` · على `29469d0` |

**النتيجة:** الـActions **مفعّل ويشتغل**. المشكلة مكانتش عطل — التريجر `push`
مكانش بيولّع على الكوميتات اللي جاية من جلسات الوكلاء. `workflow_dispatch` اشتغل
من أول مرة.

## ونتيجة التشغيل الأول: **5 خضرا · 1 أحمر**

| الوظيفة | النتيجة |
|---|---|
| ESLint (scripts) | ✅ |
| GCP config gate | ✅ |
| Mobile regression (static) — الحزمة الكاملة | ✅ |
| Production gates — chain integrity + confidence | ✅ |
| **API tests (Postgres)** — schema push + seed + السويت كامل | ✅ |
| **Typecheck & build** | 🔴 **فشل** |

### 🔴 `main` كان مكسور على الـtypecheck — من يومين

```
artifacts/api-server typecheck:
  src/services/UserService.ts(255,13): error TS2339:
  Property 'language' does not exist on type 'UpdateUserProfileInput'.
```

**المصدر:** `032bf00` (2026-08-01 19:22) — ضاف `if (input.language)` جوه الدالة،
**ونسي يضيف `language` للواجهة `UpdateUserProfileInput`**. الـzod
(`UpdateMeSchema`) كان قابله من الأول، فالجسم بيعدّي وقت التشغيل والقراءة
مبتتكمبلش.

**ليه محدش شافه؟ دي النقطة كلها:**
- الـCI معملش ران ولا مرة → مفيش حكم مستقل
- والوكلاء بيشغّلوا `typecheck` **بنطاق الموبايل بس** — والكسر في `api-server`

يعني **47+ كوميت عدّت فوق فرع مكسور**، وكل واحد فيهم مكتوب في رسالته
«typecheck نضيف». الادعاء مكانش كذب — كان **بيقيس حاجة تانية**.

**✅ اتصلح النهاردة** — `language?: "ar" | "en"` على الواجهة، مطابق للـzod.

🔗 https://github.com/waelzaid66-max/bancoboomstor/actions/runs/30841315812

> **الدرس اللي بيتحوّل لقاعدة:** «شغّلت typecheck» من غير ما تقول **أنهي نطاق**
> مش دليل. من دلوقتي: **الـCI هو الحكم**.

---

# 1) الأقسام الخمسة — الحالة

| القسم | البوليش | الحكم |
|---|---|---|
| السيارات | 423 سطر | ✅ اتنفّذ |
| المصانع | 629 سطر (ملف جديد) | ✅ اتنفّذ |
| **العقارات** | **سطر واحد** | 🔴 **مفتوح** |
| **المواد** | **صفر** | 🔴 **مفتوح** |
| **الحجز** | **سطر واحد** | 🔴 **مفتوح** — اتعمل · اتكسر · اترجع |

الخمسة **بيرندروا وشغالين**. الكلام هنا عن البوليش المطلوب، مش عن كسر.

**عيوب المالك المشخّصة من صوره — لسه موجودة كلها:**
«POWERED BY سطر لوحده» · «صفّين فلاتر» · «4 أيقونات بلا تسميات» · «أيقونة قائمة
يتيمة» · «شريحة الكل أطول من جيرانها» · «ragged wrap» · «ضغط الأزرار المفرودة».

---

# 2) بنود مفتوحة — **تحققت منها بنفسي النهاردة**

| # | البند | الأمر | النتيجة |
|---|---|---|---|
| ج-١ | أحمر قديم ناجي كـ`rgba` في الاستيراد | `grep -rn "229,\s*57,\s*53" artifacts/banco-mobile/app/import/` | **7 مواضع** — والحارس بيفحص `#RRGGBB` بس فبيعدّيها |
| ج-٢ | البحث مبيعرفش تواريخ الحجز | `grep -rn "check_in\|guests" lib/search-contract/src/` | **صفر** — والملف بيوصف نفسه «Booking.com-style» |
| ج-٣ | `profile.tsx` وحش | `wc -l "artifacts/banco-mobile/app/(tabs)/profile.tsx"` | **4445 سطر** |
| ج-٤ | سلطتان على الـschema | `artifacts/api-server/src/lib/bootstrap.ts:21` | `ensureSchemaPatches()` بتتنادى جنب الـmigrations |
| ج-٥ | قوالب `well-known` فيها `REPLACE_` | `grep -rln "REPLACE_" deploy/` | `deploy/coolify/well-known/assetlinks.json` |

---

# 3) ادعاء قديم **صححته النهاردة** — لازم يتشال من المستندات

`PROJECT_STATUS.md` (بتاريخ 2026-07-12) لسه كاتب إن **مهمة #18** حرجة:
«`financial_institution` مش في `user_role` enum».

**ده مش صحيح دلوقتي:**

```bash
sed -n '25,40p' lib/db/src/schema/index.ts        # فيه "financial_institution"
grep -rln "financial_institution" lib/db/migrations/   # موجود في 0000
```

الدور موجود في الـenum **وفي الميجريشن الأولى**. **البند مقفول** — والمستند بايت.
أي وكيل ياخد المهمة دي هيضيع وقته.

---

# 4) اللي **مقدرش** يتحقق في حاوية سحابية — وميتقالش عنه «تمام»

| البند | ليه |
|---|---|
| **الحضور (presence) بصريًا** | محتاج جلسة مسجّل دخولها + محادثات حقيقية. الكود اتشحن، **والصف نفسه — أفاتار · نقطة · مكانها — محدش شافه لسه** |
| **هيرو السيارات في اللقطة** | جوه شريحة متحركة وحالة التحميل بترسم فوقها. **لا إيجابًا ولا سلبًا** |
| **أي حكم بصري من هنا** | Clerk بيفشل في البيئة دي (محظور 9.3). **القياس الهندسي موثوق · الحكم البصري لأ** |

---

# 5) الديون المعروفة والمتروكة عمدًا

| ID | البند | الحكم |
|---|---|---|
| MSG-08b | مفيش حظر متبادل صريح | محتاج schema |
| MSG-13 | مفيش كتم لكل محادثة | محتاج schema |
| MSG-14c | مفيش مسجّل صوت | الـAPI بيقبل صوت · الواجهة لأ |
| NOTIF-02 · NOTIF-10 | تسليم الإشعارات للأجهزة غير مُصدَّق | **بيانات اعتماد خارجية — مش شغل وكيل** |
| MAP-08b | مفيش فلتر رسم مساحة | مؤجّل بقرار منتج |

---

# 6) الخلاصة

**اتحقق النهاردة:** الـCI شغال (أول تشغيل) · 5 بنود مفتوحة بأوامرها · بند واحد
اتقفل والمستند لسه بيقول مفتوح.

**لسه مش متحقق:** كل حكم بصري · الحضور على شاشة حقيقية.

**التوزيع في:** `OWNER-WORK-ORDERS-4-AGENTS-AR.md`

— المالك
