# تقرير حالة `bancoboomstor` — فحص مستقل

> **التاريخ:** 2026-08-02 · **الأساس:** `main` @ `ca5a1ed` · **المدقّق:** جلسة Claude (ويب)
> **القاعدة الحاكمة لهذا التقرير:** لا تخمين. كل سطر تحته إما أمر نُفّذ فعلاً وناتجه مذكور،
> أو أنه مكتوب صراحة تحت «غير متحقق». الادعاءات المنقولة عن جلسات سابقة مُعلَّمة كنقل، لا كإثبات.

---

## 0. حدود الفحص — اقرأ ده الأول

الحاوية اللي اتنفّذ فيها الفحص **ما فيهاش `node_modules`** (`pnpm install` لم يُشغَّل).
النتيجة المباشرة: **لا typecheck، ولا مجموعة اختبارات api-server، ولا أي اختبار يحتاج تبعيات.**

اللي أمكن تشغيله فعلاً هو الحرّاس الثابتة (source scanners) اللي بتشتغل بـ `node --test` من غير تبعيات.
أي بند تحت «غير متحقق» في القسم ٦ **مش معناه إنه سليم ولا معطوب — معناه إنه لسه مجهول.**

---

## 1. الريبو والهجرة

| البند | النتيجة | الدليل |
|---|---|---|
| عدد الـcommits الكلي | **20** | `git log --oneline \| wc -l` |
| بداية الريبو | `89d28d3` — *initialise bancoboomstor as the single production source of truth* | `git log` |
| رأس `main` | `ca5a1ed` (2026-08-02 17:20:18 UTC) | `git log -1 origin/main` |
| شجرة العمل | **نظيفة** | `git status --porcelain` → فارغ |
| علامات تعارض دمج | **صفر** | `git grep -E '^(<<<<<<<\|>>>>>>>) '` → فارغ |
| فرع `claude/boom-car-hero-header` | **مدموج بالكامل** | `git log origin/main..origin/claude/boom-car-hero-header` → فارغ |
| عدد الـPRs في الريبو | **صفر** (بكل الحالات) | `list_pull_requests state=all` → `[]` |

**استنتاج:** كل الدمج تم بـ push مباشر على `main`. لا يوجد سجل مراجعة لأي تغيير في هذا الريبو.

### نظام الـmigrations

| البند | النتيجة | الدليل |
|---|---|---|
| جداول في `0000_fantastic_warbird.sql` | **71 `CREATE TABLE`** | `grep -c 'CREATE TABLE'` |
| جداول في `schema/index.ts` | **71 `pgTable(`** | `grep -c 'pgTable('` |
| SQL مدمّر في الـmigrations | **صفر** (`DROP TABLE` / `DROP COLUMN` / `TRUNCATE` / `DELETE FROM`) | `grep -icE` على الملفين |
| `0001_minor_stingray.sql` | `ALTER TABLE "users" ADD COLUMN "language" text;` — إضافي بالكامل | قراءة الملف |
| الـjournal | مُسجّل، إدخالان مرتّبان (idx 0, 1) | `meta/_journal.json` |

**عدد الجداول متطابق (71 = 71).**

### `push --force`

الادعاء في `9f3e5c5` صحيح ومُتحقق منه: المسارات الأربعة اللي بتلمس قاعدة بيانات حقيقية
(`scripts/post-merge.sh` · `docker-compose.coolify.yml` · `docker-compose.prod.yml` · `deploy/aws/scripts/db-migrate.sh`)
اتحوّلت لـ `run migrate`.

الاستثناءان الباقيان **مشروعان ومُتحقق منهما بالقراءة**:
- `ci.yml:86` و `deploy.yml:65` — الاتنين جوه job بيشغّل `services: postgres: image: postgres:16` (حاوية مؤقتة تُهدم بعد الرن)، مش قاعدة بيانات حقيقية.

---

## 2. آخر شغل قبل التوقف

فرع واحد غير مدموج: **`claude/headers-dynamic-polish`** — متفرّع من `ca5a1ed` مباشرة، **بلا PR**.

| الوقت (UTC) | Commit | ملفات |
|---|---|---|
| 17:33:57 | `ea71942` feat(search): السماح للـcaller يحط chrome جوه قائمة النتائج | 2 |
| 17:47:41 | `a214482` feat(cars): الهيرو يمشي مع السكرول والبحث يفضل مثبّت | 2 |
| 18:10:15 | `239761c` fix(cars): ربط الأكسنت بأحمر الهوية | 2 |
| 18:13:12 | `dcc2c1a` fix(headers): منع الـtop inset من السالب على أندرويد | 2 |
| 18:26:21 | `58d45be` assets: لوحات الهيرو من رندرات المالك | 2 صور |
| 18:28:47 | `ba3a266` docs(header): تسجيل المواصفة والتعارضات الثلاثة | 1 |

الإجمالي مقابل `main`: **9 ملفات · +305 / −12 سطر**.

**آخر commit توثيق مش كود.** الشغل وقف عند نقطة قرار، مش عند عطل.

---

## 3. مرحلة الهيدرات — المنفَّذ مقابل غير المنفَّذ

### منفَّذ (مقروء من الكود على الفرع)
- `SearchResultsSurface` اكتسب `listHeader?: React.ReactElement | null` → يتمرّر لـ `ListHeaderComponent`، و`scrollEnabled` بقى `items.length > 0 || !!listHeader`.
- `CarsHomeHeader` اكتسب `slot?: "all" | "pinned" | "scroll"` مع `showPinned` / `showScroll` — أي الهيدر اتقسم: شريط مثبّت + هيرو يمشي مع القائمة.
- `SectionSearchApp` بيبني `carScrollHeader` ويمرّره للقائمة.
- الأكسنت مربوط بـ `sectionAccent.car = "#CC1E24"` (قيمة مؤكدة في `lib/sectionTheme.ts:17`) — مش رقم مكتوب في المكوّن.

### غير منفَّذ (فحص نصي قاطع على الفرع)
- **الانهيار المتحرك غير موجود إطلاقاً:** `useAnimatedScrollHandler` · `Animated.FlatList` · `interpolate(` · `useSharedValue` → **صفر نتائج** في `components/search`.
- **`expo-blur` / `BlurView` غير مستخدمة** في `components/search` → صفر نتائج.
- **`#E60012` غير مُتبنّى في الكود** — ذِكره الوحيد داخل تعليق يشرح سبب رفضه.

### القرارات الثلاثة المعلّقة (منقولة من `DYNAMIC-HEADER-SPEC-AR.md` — قرارها قرارك)
1. **الأحمر:** `#E60012` (المواصفة) أم `#CC1E24` (الكود الحالي)؟
2. **«SVG فقط»:** تنطبق على الأيقونات فقط أم على الشعارات كمان؟
3. **الانهيار المتحرك:** يُبنى بـ Reanimated (وده يلغي اختيار «الحل الأأمن» السابق) أم يفضل الحل الحالي؟

---

## 4. النتائج — مرتبة بالخطورة

### ن-١ · لم تُشغَّل أي CI على هذا الريبو ولا مرة واحدة
أربعة workflows نشطة ومسجّلة منذ 2026-08-01 (`CI` · `Deploy (AWS)` · `CI Website` · `CI Website Docker`)،
وعدد التشغيلات: **`{"total_count":0}`**.

معناها: **الـ20 commit كلها دخلت `main` بدون أن يتحقق منها أي جهاز آلي في هذا الريبو.**
كل الأرقام الخضراء في التقارير القديمة (246 اختبار · 0 typecheck · 5/5 jobs) تخصّ الريبو **القديم**، لا هذا.

### ن-٢ · حارسان موجودان لكن لا يعملان في أي مكان
`artifacts/banco-mobile/tests/car-hero-honesty-guard.test.mjs` و `auth-routes.test.mjs`
**غير مذكورين في سلسلة `test`** في `artifacts/banco-mobile/package.json`
(بحث على كامل الريبو: ذِكرهما الوحيد داخل تعليق في الملف نفسه).
وبما أن `ci.yml:135` و `deploy.yml:55` بيشغّلا السلسلة دي بالذات → **الحارسان لا يُنفَّذان أبداً**.

وثيقة المواصفة تقول إن حارس الصدق «بيفشّل البيلد لو رجعت الأرقام الوهمية» — **ده غير صحيح حالياً**.
نفس الثغرة المسجّلة في `.agents/memory/banco-icon-validation-gap.md`. الإصلاح: سطر واحد.
(شغّلتهما يدوياً: **5/5** و **3/3** ناجحة.)

### ن-٣ · 1.29 ميجابايت أصول ميتة على فرع الهيدرات
`section-hero/car.png` (1,102,362 بايت) و `materials.png` (198,868 بايت) مضافين في `58d45be`،
و**مش مُشار إليهما من أي ملف كود** (بحث `section-hero|car\.png|materials\.png` على كل `artifacts/banco-mobile` → صفر).
يعني: صور اتحطت استعداداً لخطوة لم تُنفَّذ بعد. تُوصَل أو تُشال — لا تُترك.

### ن-٤ · سلطتان على الـschema تعملان معاً
`ensureSchemaPatches()` لسه بتتنادى في كل إقلاع (`artifacts/api-server/src/lib/bootstrap.ts:21`)،
بجانب نظام الـmigrations الجديد. و`lib/db/MIGRATIONS.md` نفسها بتوصف الملف ده بأنه
«نظام migrations بلا ترقيم ولا ترتيب ولا سجل — معاد تنفيذه يدوياً».
مش عطل النهارده، لكنه مصدر حقيقة ثانٍ للـschema ومحتاج قرار.

### ن-٥ · وثائق تناقض الكود
- `PROJECT_STATUS.md` بيقول الباك-إند **Hono** — الحقيقي **Express 5** (`artifacts/api-server/package.json`).
- `BANCO_MASTER_REFERENCE.md` و`DUAL_REPO_STATUS.md` مبنيان على `-BANCO-CA-OOM-` و`aws-virgen` كمصدر حقيقة، وده لم يعد صحيحاً.

---

## 5. ما تم تشغيله فعلاً ونتيجته

| الفحص | `main` | فرع الهيدرات |
|---|---|---|
| `scripts/chain-integrity-gate.mjs` | ✅ **198/198** | ✅ **198/198** |
| `car-hero-honesty-guard` | ✅ 5/5 | ✅ |
| `auth-routes` | ✅ 3/3 | — |
| `section-miniapp-guard` · `ui-density-guard` · `stay-honesty-guard` | ✅ | ✅ |
| `icons` | ⛔ تعذّر | ⛔ تعذّر |
| `i18n-usage` | ⛔ تعذّر | ⛔ تعذّر |

⛔ **ليست إخفاقات كود.** `icons` بيسقط على `Cannot find module '@expo/vector-icons/package.json'`،
و`i18n-usage` على `error TS5112` من `tsc`. الاتنين **يسقطان بنفس الشكل على الفرعين** — أي **ليس انحداراً** من شغل الهيدرات، لكنه كمان **ليس دليل نجاح**.

---

## 6. غير متحقق — بصريح العبارة

1. **هل قاعدة بيانات الإنتاج اتعمل لها baseline؟** خطوات التبنّي في `MIGRATIONS.md` تتطلب `DATABASE_URL` حي. لا يوجد في هذه البيئة. **مجهول.**
2. **هل الهجرة من `-BANCO-CA-OOM-` نقلت كل الملفات؟** الريبو القديم غير مربوط بالجلسة، فلا مقارنة شجرة تمّت. **مجهول** — قابل للحسم بربطه وعمل diff.
3. **typecheck ومجموعة api-server (246+).** غير مُشغَّلة (لا تبعيات). **مجهول.**
4. **قياس «465dp ← 136dp»** و**استخلاص `#CF1626` من الشعار**: منقولان عن الجلسة السابقة (تعليق في `CarsHomeHeader.tsx` + الوثيقة). **لم أُعِد قياسهما.**
5. **حالة الأصول:** عقارات ❌ ناقصة · حجز/مصانع لسه عند المالك — منقول عن الوثيقة، لا يمكن التحقق منه برمجياً.

---

## 7. المطلوب حسمه

| # | القرار | يفتح |
|---|---|---|
| ١ | تشغيل CI مرة على `main` (workflow_dispatch) | حسم البند ٣ في «غير متحقق» بالكامل |
| ٢ | ضم الحارسين لسلسلة `test` | إغلاق ن-٢ — سطر واحد |
| ٣ | دمج `claude/headers-dynamic-polish` أم انتظار القرارات الثلاثة | استكمال مرحلة الهيدرات |
| ٤ | الأحمر · قاعدة SVG · الانهيار المتحرك | تحرير تنفيذ الهيدر |
| ٥ | ربط الريبو القديم للمقارنة | حسم البند ٢ في «غير متحقق» |
| ٦ | مصير `ensureSchemaPatches` | إغلاق ن-٤ |

*لا شيء من هذا نُفِّذ. التقرير قراءة وتشغيل فقط — لا تعديل على أي كود.*
