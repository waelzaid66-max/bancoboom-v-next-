# إلى الإيجنت الأساسي — إحالة كل المشكلات المفتوحة

**من:** الإيجنت اللي شغّال على `claude/headers-dynamic-polish`
**التاريخ:** 2026-08-02
**بأمر المالك:** *«هتحيل كل المشكلات إلى الإيجنت الأساسي فورًا بكل تقاريرك»*

ده **فهرس إحالة**، مش تقرير جديد. كل مشكلة تحت ليها مصدر كامل في ملف تاني —
اقرا المصدر قبل ما تلمس أي حاجة.

---

## ⚠️ تصحيح لازم أقوله عن نفسي قبل أي حاجة

قريت `PRODUCTION-MANAGER-BRIEF-AR.md` **بعد** ما كتبت النسخة الأولى من الملف ده.
المحظور **⑨.٣** فيه بيخصّني مباشرة:

> *«التطبيق لا يبعث أي طلب بحث في بيئة التطوير… لا تقبل من أي حد لقطة شاشة من
> البيئة دي كدليل على الشكل النهائي.»*

**اللقطات اللي في القسم 4 تحت طالعة من البيئة دي بالظبط**، و`Clerk` فعلًا فشل
عندي («did not initialize… rendering as signed-out»). فالقاعدة تنطبق عليها.

**بس البريف نفسه بيفرّق:** *«قياس الأرقام والمقاسات موثوق ✅ · الحكم البصري
النهائي مستحيل هنا ❌»*.

واللي اكتشفته **قياس هندسي مش حكم بصري**: عرض حاوية بالـdp، وعلامة بتتقص لأن
عرضها أكبر من حاويتها. ده جوه الخانة الموثوقة. **فالنتيجة صامدة، لكن:**

- ✅ **مقبول كدليل:** الأرقام (14.4 · 54.4 · 84.4 · 124.4) وواقعة القص نفسها
- ❌ **مش مقبول كدليل:** أي حكم على اللون النهائي أو الإحساس أو التباين

**محتاج تأكيد على جهاز حقيقي قبل ما تعتبر الشكل مقفول.**

---

## 0) ملفاتي التلاتة — اقراهم بالترتيب ده

| # | الملف | فيه إيه |
|---|---|---|
| ① | `DYNAMIC-HEADER-SPEC-AR.md` | مواصفة الهيدر + **التلات تعارضات وقد اتحسمت** بقرار المالك |
| ② | `APP-WIDE-UX-AUDIT-2026-08-02-AR.md` | **الفحص الشامل** — 63 شاشة · 57 مكوّن · كل بند بملف وسطر |
| ③ | الملف ده | فهرس الإحالة + اللي اتقفل واللي لسه مفتوح |

وملف التسليم اللي وصلني منك — `TO-CLAUDE-AGENT-ON-REPO-HEADERS-HANDOVER-AR.md` —
اتقرا بالكامل والتزمت بيه. الفخاخ التلاتة اللي حذّرت منها **كلها محترمة**
(مكتوب إزاي في ①).

---

## 1) ✅ اللي اتقفل — متفتحوش تاني

### قرار ٢ في ملفك (الأحمر `#E60012` ولا `#CC1E24`؟) — **اتحسم: `#CC1E24`**

نص المالك: *«الألوان أقرب ما يكون للهوية للأبلكيشن ككل»*.

قياس مستقل أكّد أرقامك وزوّد الفرق المحسوس:

| مقارنة | ΔE |
|---|---:|
| `#CC1E24` ↔ اللوجو `#CF1626` | **2.4** — تحت عتبة الملاحظة |
| `#E60012` ↔ اللوجو | **16.7** — أحمر مختلف بوضوح |

الأكسنت فضل **مربوط** بـ`sectionAccent("car")`، والتوهج والأكسنت الفاتح
**مشتقين منه**. باقي لوحة البريف اتاخدت حرفيًا.

### قرار ٣ (نطاق «NO PNG») — **اتحسم: الهيرو متجهي ويتقوّى**

**تصحيح لجردك:** `section-hero/car.png` و `materials.png` **مش مستخدمين في أي
مكان في الكود**. الهيرو أصلًا `react-native-svg` خام. القاعدة كانت متحققة أكتر
بكتير مما الجرد بيقول. اتبنى فوقها: تدرج طبقي · vignette · نفس المعالجة مصغّرة
للشريط المثبّت. صفر راستر جديد.

### قرار ٤ (Reanimated يلغي الحل الحالي؟) — **اتحسم: لأ، اتبنى فوقه**

**تصحيح مهم:** البند كان بيفترض إن الهيدر لازم «يرجع بره القائمة بموضع
`absolute`». **مش مطلوب** — الشريط المثبّت أصلًا بره `FlatList` من يوم ما قسمت
`slot`. شغلك مااتلغاش.

اللي اتضاف: prop **اختياري** `scrollY` في `SearchResultsSurface` بنفس نمط
`listHeader` بالظبط + `useAnimatedStyle` في الهيدر. صفر re-render أثناء السكرول.

### 🔴 البند الوحيد من البريف اللي **ماتنفّذش عن قصد** — التمويه

الشريط المثبّت **شقيق** للقائمة مش فوقها، فمفيش محتوى بيمر تحته عشان يتموّه.
`BlurView` هناك = ديكور كداب. عشان يبقى حقيقي لازم الشريط يبقى `absolute` فوق
القائمة — وده التغيير الكبير اللي المالك منعه. **موثّق في ①، مش متنسي.**

---

## 2) 🔴 مشكلات محالة ليك — مرتّبة بالخطورة

المصدر الكامل لكلها: **②**.

### ٢.١ أكبر أصل ميت في المشروع — المطوّرين والكمبوندات

`lib/db/src/schema/index.ts:2546` فيه `reference_developers` كامل ومزروع بأسماء
حقيقية (طلعت مصطفى · إعمار مصر · SODIC).

- **مفيش endpoint** بيقراه
- **مفيش شاشة** بتعرضه
- والتعليق فوقه بيقول «عشان المشروع (الكمبوند) يربط بمطوّر واحد» —
  **والمشروع ده مش موجود أصلًا**

و`reference_places:2573` عنده الهرم جاهز: `… community · compound · phase ·
building · unit` — **ومش مستغل**.

النهاردة الكمبوند = **شيك بوكس** (`lib/search-contract/src/engines.ts:214`).

**ملاحظة تخص الزحام:** ده **ضد** الزحام مش معاه. دلوقتي كل وحدة في كمبوند =
دبوس منفصل، فالخريطة بتتزحم بمية دبوس لنفس المكان. المشروع ككيان = دبوس واحد
يفتح على وحداته. **تبسيط.**

🔶 **مستني قرار المالك.** أصغر خطوة مفيدة: توصيل الجدول الموجود بـendpoint
وصفحة مطوّر، من غير لمس أي شاشة قايمة.

### ٢.٢ الخريطة — ناقصة أهم ميزتين في السوق

القاعدة قوية (Leaflet **محلي مش CDN** · تجميع من السيرفر · viewport). المشكلة
إنها **خريطة عامة** في سوق بيتنافس بخرايط متخصصة.

**الأهم: رسم المنطقة (draw-to-search).** المشتري مابيدوّرش بنصف قطر، بيدوّر
بحدود يعرفها — والدايرة الحالية بتدخّل مناطق هو رافضها. التكلفة عندنا **منخفضة**:
`Leaflet.draw` يتضمّن زي `MarkerCluster` بالظبط، والسيرفر أصلًا بياخد `bounds`
فياخد polygon. بعدها **زمن الوصول (isochrone)**.

🔶 **المالك وافق على البند ده** (اختار «3» في ترقيم تقريري = رسم المنطقة).
راجع القسم 4.2 في ② للتفاصيل الكاملة.

### ٢.٣ الهوية اتكسرت في الاستيراد — أوسع مما في ملفك

ملفك قال ملف واحد. **الحقيقة ٦ ملفات:**

```
app/import/calculator.tsx:22 · app/import/order/[id].tsx:32
app/import/auctions.tsx:22   · app/import/index.tsx:32
app/import/documents.tsx:21  · components/import/OrderDocuments.tsx:34
        const RED = "#E53935";      ← أحمر Material الافتراضي · ΔE ≈ 19 عن اللوجو
```

**وأسوأ:** `app/import/order/[id].tsx:41-46` فيه **لوحة قوس قزح** كاملة
(أحمر · برتقالي · أصفر · أزرق · بنفسجي · أخضر) بتكسر القاعدة المقفولة نصًا.

**اقتراحي:** مرحلة الشحن حالة **تقدّم** مش قسم. التقدّم يتقال بالامتلاء مش
باللون — مسار رمادي، المكتمل بأحمر الهوية، الحالي أفتح، الجاي رمادي. الأخضر
للتسليم بس لأنه الاستثناء المفهوم عالميًا. 🔶

### ٢.٤ الزحام — `profile.tsx`

**49 ضغطة** في 4445 سطر. أسوأ شاشة في التطبيق. بتخلط تلات عوالم (شخصي · تجاري ·
إعدادات) في سطح واحد.

باقي الشاشات كويسة فعلًا: `search` بـ9 · `settings` بـ11 · `index` بـ11.

**اقتراحي (بساطة مش حذف):** تلات بطاقات دخول، وكل عالم يفتح على سطحه. نفس
المميزات بالظبط، تلات قرارات بدل تسعة وأربعين. 🔶

### ٢.٥ اتساق النيوترالز — دَين صغير عملته أنا، مكتوب بأمانة

هيدر السيارات بقى `VOID #090909` · `ASH #A5A5A5` · فاصل `0.06`.
إخواته التلاتة لسه `#000000` / `#8E8E93`:

```
components/search/property/PropertyHomeHeader.tsx:35-37
components/search/materials/MaterialsHomeHeader.tsx:35-37
components/search/stays/StaysHomeHeader.tsx:29-31
```

مقبول مؤقتًا لأنهم في الطابور، **بس لازم تاخد بالك لما ييجي دورهم** وإلا
التطبيق يفضل بلوحتين. الصح إن النيوترالز تطلع لملف توكنز واحد بدل أربع نسخ.

### ٢.٦ حارس النزاهة ناقص تغطية

`tests/car-hero-honesty-guard.test.mjs` بيحمي **السيارات** بس. نفس نوع الغلط
عاش في الاستيراد (`"8+"` و`"21"` — اتشالوا مني النهاردة). **الحارس محتاج يتوسّع
ليشمل `app/import`** وإلا هيرجع.

---

## 3) ✅ اللي نفّذته أنا — عشان متكررهوش

كله على `claude/headers-dynamic-polish`، والبوابات التلاتة خضرا بعد كل كوميت.

| الكوميت | إيه |
|---|---|
| `feat(cars)` | الهيدر المتحرك — انهيار 94→60 · لوجو 82% · لوحة البريف · مقاسات |
| `docs(header)` | تصحيح عدد الاختبارات + توثيق الفخاخ التلاتة |
| `fix(app)` | **الهوية الصوتية** + شيل `compact` الميت + شيل الأرقام الوهمية |
| `fix(home)` | **علامة B-OOM** — أكبر 2.06mm + وقف القص |

### اتنين منهم يهموك بالذات

**الهوية الصوتية.** `SoundContext` كاتب نيّته من أول يوم — «engine للمركبات ·
key للعقارات» — و**كل الـ68 نداء في التطبيق كانوا `tap`**. يعني 110KB صوت
محمولة في الباندل و**ولا مرة اشتغلت**. `soundForCategory()` هي الخريطة اللي
كانت ناقصة. `app/(tabs)/search.tsx` اتساب على `tap` **عن قصد** — مضيف بيخلط
أقسام، فصوت قسم واحد فيه ادعاء كاذب.

**علامة B-OOM.** المالك قال «كبّرها 2mm، مش باينة». الفحص البصري طلّع إن الحجم
نص المشكلة بس — راجع القسم 4.

---

## 4) 🔬 الفحص البصري — طريقة اشتغلت، استخدمها

المالك طلب **فحص بصري حقيقي**. عملته وهي شغالة، فمتضيعش وقت تكتشفها تاني:

```bash
# 1) إصلاح البيئة — Metro مش لاقي expo من الجذر (مشكلة رفع pnpm)
ln -sfn "$PWD/artifacts/banco-mobile/node_modules/expo" node_modules/expo

# 2) البناء
cd artifacts/banco-mobile && pnpm build:web

# 3) التقديم — لازم BASE_PATH يطابق البناء وإلا الصفحة بيضا و404 على الباندل
PORT=3111 BASE_PATH=/banco-mobile node server/serve.js
# افتح http://localhost:3111/banco-mobile/   ← مش على الجذر

# 4) التصوير — Chromium متثبت مسبقًا، متعملش playwright install
#    executablePath: "/opt/pw-browsers/chromium"
```

Clerk بيرمي «did not initialize» ويرندر التطبيق **signed-out** — ده كفاية
للفحص البصري للكروم والهيدرز.

### اللي الفحص ده كشفه — وماكانش ممكن يتكشف من الكود

`styles.spark` عليه `flex:1` + `overflow:hidden`، فبياخد المتبقي بس. والصف
بيصرف 103.6dp على لوجو بانكو + 122dp على تلات أزرار + 48dp على أربع فجوات.
مقيس في Chromium على أربع عروض حقيقية:

| الشاشة | مساحة spark | العلامة قبل (43dp) | العلامة بعد |
|---|---:|---|---|
| 320dp | **14.4** | 🔴 اترندرت `·OC` — شظية | ✅ كلمة كاملة صغيرة |
| 360dp | 54.4 | تعدّي بصعوبة | 49dp كاملة |
| 390dp | 84.4 | 46dp | **56dp** |
| 430dp | 124.4 | 47dp | **56dp** |

**يعني على 320dp المالك ماكانش بيبص على علامة صغيرة — كان بيبص على تلتها.**
والتكبير لوحده كان هيقص أكتر.

الحل: العلامة **تصغّر لتناسب** بدل ما تتقص، بسقف 90% من المساحة. والـ90% مختارة
مش مدوّرة: `0.9 × 1.10` (قمة النبض) `= 0.99` — فالنبض عمره ما يوصل للقص، ولا
للوجو اللي القص موجود يحميه. النبض نفسه **ماتلمسش**.

---

## ٤.٥) 🔗 المطابقة مع بريف مدير الإنتاج — عشان نبقى على نفس الورقة

مطابقة مباشرة على `PRODUCTION-MANAGER-BRIEF-AR.md`. **ده الجدول اللي يتقرا لو
مفيش وقت لحاجة تانية.**

### القرارات السبعة المفتوحة (قسم 11 في بريف المدير)

| # | البند في البريف | الحالة دلوقتي | بيفتح إيه |
|---|---|---|---|
| 1 | 🚨 تدوير مفتاح الدفع | 🔴 **مفتوح** — المالك حصريًا · ماقربتش منه | أمن الإنتاج |
| 2 | الأحمر `#CC1E24` ولا `#E60012`؟ | ✅ **اتقفل → `#CC1E24`** | **يفتح توحيد ألوان الاستيراد (بند 8 في ترتيب المدير)** |
| 3 | «NO PNG» نطاقها إيه؟ | ✅ **اتقفل** — الهيرو متجهي أصلًا · فاضل الشعارين بس | يفتح بوليش باقي الهيدرز |
| 4 | رندر مسطّح للعقارات | 🔴 **مفتوح** — المالك | لوحة B-PROPERTIES |
| 5 | الوردمارك جوه الشريط العلوي؟ | 🔴 **مفتوح** — **أهم بند** | يفك العقبة البنيوية للتلات هيدرز |
| 6 | أرقام الإحصائيات | 🟡 **جزئي** — شلت `"8+"` و`"21"` من الاستيراد · المصدر لسه مفتوح | يحتاج endpoint |
| 7 | باج `rental_term` | ✅ اتصلح قبلي (`7bde362`) | — |

**الخلاصة: اتنين من السبعة اتقفلوا (2 و3)، وواحد اتحرك (6).**

### ترتيب التنفيذ (قسم 12 في بريف المدير)

| # | البند | الحالة |
|---|---|---|
| 1 | تدوير مفتاح الدفع | 🔴 المالك |
| 2 | ختم قواعد البيانات (`baseline`) | 🔴 لسه — ماقربتش من الداتابيز |
| 4 | حسم القرارات 2·3·4·5 | 🟡 **2 و3 اتقفلوا** · 4 و5 لسه |
| 5 | **الهيدر المتحرك للسيارات — «النمط المرجعي»** | ✅ **خلص** — كوميت `7503dba` |
| 6 | العقارات ← المواد ← الحجز ← المصانع | ⛔ مقفول على قرار 5 · المصانع بس يقدر يبدأ |
| 7 | ضغط أزرار الدول | 🟡 شلت `compact` الميت · الضغط نفسه لسه |
| 8 | بوليش الاستيراد | 🟢 **بقى مفتوح** لأن قرار 2 اتقفل — أرقامه الوهمية اتشالت خلاص |

**البند 5 «النمط المرجعي» اتنفّذ.** يعني النمط اللي باقي الهيدرز تتبعه بقى
موجود في `CarsHomeHeader.tsx` — اقراه قبل ما تبدأ في أي هيدر تاني.

### الاثنان اللي بقوا جاهزين للبدء فورًا

1. **بوليش الاستيراد** — كان مقفول على قرار 2، والقرار اتقفل على `#CC1E24`.
   الستة `RED = "#E53935"` وقوس القزح جاهزين للتوحيد.
2. **هيدر المصانع** — مستثنى من العقبة البنيوية (مالوش هيدر أصلًا)، فيتبني
   بالنمط الصح من أول لحظة. **متعملهوش بحذف الهيدر العام** — أقسام تانية بتستخدمه.

### التزامي بالمحظورات الاثنعشر (قسم 9)

اتأكدت واحد واحد قبل الكوميت:

| المحظور | حالتي |
|---|---|
| ① خلط فلاتر الأقسام | ماقربتش من `useSearchMiniApp` |
| ② بيانات وهمية | **شلت** وهمي، ماضفتش |
| ③ ممنوع الحذف · كل `testID` يفضل | ✅ اتأكدت — `section-back` اتلف جوه حاوية جديدة **ومفضل موجود** (سطر 386). صفر `testID` اتشال |
| ④ ممنوع كتابة `testID` محظور حتى في تعليق | ماكتبتش ولا واحد · الحرّاس عدّت 294/294 |
| ⑤ ممنوع handler فاضي | صفر — اتأكدت بالـdiff |
| ⑥ `push --force` على داتابيز | ماقربتش من الداتابيز |
| ⑦ إعادة sync workflows | ماعملتش |
| ⑧ `pkill -f "expo"` | ماعملتهاش. **بس تنبيه:** عملت `pkill -f "node server/serve.js"` ورجّعت خروج 144 وضربت السيرفر. **استخدم PID محدد حتى للسيرفر.** |
| ⑨ `tsc` المحلي | كل النداءات عبر `pnpm typecheck` |
| ⑩ `tsc --build lib/*` قبل الموبايل | السكربت بيعملها جوّه |
| ⑪ `pg_trgm` | ماقربتش من الداتابيز |
| ⑫ `push-force` | ماستخدمتش |

**إضافة على قائمة البيئة (قسم 10) — مصيدتين جداد اتكشفوا عندي:**

| المصيدة | الحل |
|---|---|
| `pnpm build:web` بيفشل بـ`Cannot find module 'expo/package.json'` | `@expo/metro-config` في الجذر و`expo` مرفوع عند الموبايل بس. `ln -sfn "$PWD/artifacts/banco-mobile/node_modules/expo" node_modules/expo` (خارج الريبو، مش تغيير) |
| الصفحة بيضا و404 على الباندل | البناء بيبيك `BASE_PATH=/banco-mobile` جوّه `index.html`. لازم تقدّم بنفس القيمة وتفتح `/banco-mobile/` مش الجذر |

---

## 5) الترتيب اللي أوصي بيه

| # | الشغل | مخاطرة | قرار؟ |
|---|---|---|---|
| 1 | توسيع حارس النزاهة لـ`app/import` | منخفضة | لأ — **ابدأ بيه** |
| 2 | توحيد أحمر الاستيراد + تقدّم الشحنة | منخفضة | 🔶 |
| 3 | **رسم المنطقة على الخريطة** | متوسطة | ✅ **المالك وافق** |
| 4 | نيوترالز موحّدة للهيدرات الأربعة | منخفضة | مع التصميمات الجاية |
| 5 | إعادة تنظيم `profile` بدون حذف | متوسطة | 🔶 |
| 6 | **المطوّر ككيان** (endpoint + صفحة) | متوسطة | 🔶 |
| 7 | **المشروع/الكمبوند ككيان** | عالية | 🔶 |

---

## 6) اللي لسه مقفول عندك — قرار ٥ في ملفك

**الـwordmark ينتقل جوه التوب بار في العقارات والمواد والحجز؟**

ده لسه **مقفّل التلات هيدرز**. المالك قال إن عنده **تلات تصميمات جاية** للتلاتة
دول — فالقرار ده هيتاخد معاهم. المصانع بس هو اللي يقدر يبدأ دلوقتي لأنه مالوش
هيدر أصلًا.

— إحالة كاملة · `claude/headers-dynamic-polish` · 2026-08-02
