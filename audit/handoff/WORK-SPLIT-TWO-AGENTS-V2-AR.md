# تقسيم العمل — النسخة ٢ · بعد التصادم

**المستودع:** `waelzaid66-max/bancoboomstor` · **الأساس:** `main` (`2a81157`)
**التاريخ:** 2026-08-02 · **صادر بصلاحية المالك**
**يلغي:** `AGENT-WORK-ORDERS-PARALLEL-AR.md` (النسخة ١)

---

# 0) 🔴 ليه فيه نسخة تانية — الدرس اللي كلّفنا شغل

النسخة ١ قسّمت **بالملفات**. وده فشل عمليًا. النهاردة الوكيلين عملوا **نفس الشغل** في تلات مواضع:

| البند | الوكيل الأساسي | الوكيل التاني | النتيجة |
|---|---|---|---|
| شيل الأرقام الوهمية في الاستيراد | `3701042` | `21779c1` | 🔴 تعارض |
| شيل الـ prop الميت `compact` | `c643d04` | `21779c1` | 🔴 تعارض |
| الملف المشترك `SectionSearchApp` | فرع المصانع | `scrollY` | 🔴 تعارض |

**السبب الحقيقي:** الاتنين اتفرّجوا على نفس التقارير، فاختاروا نفس «التنضيف الواضح».
**الملكية بالملف مامنعتش ده** — لأن مفيش حد كان بيملك «الأرقام الوهمية» كبند.

## القاعدة الجديدة — الملكية بالنطاق مش بالملف

**كل وكيل بيملك عالم كامل: ملفاته + بنوده + حراسته.**
**وأي بند مش مكتوب في نطاقك = مش بتاعك، حتى لو شفته وحتى لو إصلاحه سطر واحد.**

### وبروتوكول الحجز — إلزامي قبل أي كوميت

قبل ما تبدأ أي بند، اكتب في `CHANNEL-OWNER-CLAUDE-LIVE-AR.md`:

```
حجز: <رقم البند> · <الوكيل> · <التاريخ> · <الملفات اللي هتلمسها>
```

**لقيت حد حاجز بند؟ متلمسوش. سيبه واكتب ملاحظتك في القناة.**

---

# 1) الوكيلان — تقسيم النطاقات

| | **الوكيل A** (الأساسي) | **الوكيل B** (المتحرك) |
|---|---|---|
| **العالم** | الحجز · الاكتشاف · التوريد | الحركة · الهوية · الاستيراد |
| **ليه** | `BookingStaysApp` مستقل معماريًا، و`SearchDiscover` سطح واحد مغلق | بنى نمط الأنيميشن، ولقى الأحمر في الستة ملفات، وعنده طريقة فحص بصري شغالة |

## ملكية الملفات — حصرية وقاطعة

| الوكيل A | الوكيل B |
|---|---|
| `components/SearchDiscover.tsx` | `components/search/car/**` |
| `components/search/BookingStaysApp.tsx` | `components/search/property/**` |
| `components/search/stays/**` | `components/search/materials/**` |
| `app/bookings.tsx` · `app/rentals/**` | `app/import/**` · `components/import/**` |
| `components/search/facilities/**` | `context/SoundContext.tsx` |
| `app/(tabs)/search.tsx` | `lib/sectionTheme.ts` · التوكنز |

## 🔴 الملف المشترك الوحيد

```
components/search/SectionSearchApp.tsx   (3070 سطر)
```

| # | القاعدة |
|---|---|
| 1 | **إضافات معزولة بس** — فرع أو prop. ⛔ ممنوع إعادة هيكلة من أي حد |
| 2 | `git pull --rebase origin main` **قبل كل كوميت** |
| 3 | كوميت صغير ومتكرر — ⛔ ممنوع تجميع شغل ساعات |
| 4 | تعارض؟ **الأسبق يفوز، والتاني يعيد التطبيق فوقه.** ⛔ ممنوع `--force` |
| 5 | ⛔ **ممنوع تلمس `useSearchMiniApp`** — المحرّك المشترك |

---

# 2) 🅑 مهام الوكيل B — قوية ومحددة

## B-1 · تعميم الانهيار المتحرك على التلات هيدرز 🔴 **الأكبر**

**انت بنيت النمط في السيارات. عمّمه.**

**المرجع الجاهز:** `components/search/facilities/FacilitiesHomeHeader.tsx` — نموذج **الشرائح الثلاث**:
```
pinned = topBar + brandLockup(صف واحد) + search
scroll = hero + strips + counts
```

**ده بيحل العقبة اللي وقّفت التلاتة:** البراند في العقارات والمواد والحجز **بره** `topBar`
(عقارات 270 أخ لـ 208 · سيارات 245 **ابن** لـ 229). توسيع الشريحة المثبّتة تشمل البراند
**بيخلي BANCO فوق الفلاتر من غير ما نحرّك عقدة واحدة في الشجرة.**

| # | القسم | الملف | الارتفاع → الهدف |
|---|---|---|---|
| 1 | العقارات | `property/PropertyHomeHeader.tsx` (793) | ~309dp → **≈130dp** |
| 2 | المواد | `materials/MaterialsHomeHeader.tsx` (477) | ~180+105dp → أقل |

⛔ **الحجز مش عندك** — بتاع الوكيل A (`stays/**`).

**المترتب على القرار:**
- ✅ الشاشة تتحرر في قسمين كمان
- ⚠️ `scrollY` هيحتاج يتوسّع في الملف المشترك → **اتبع بروتوكول الملف المشترك**
- ⚠️ ضغط `brandBlock` من 3 صفوف لصف واحد **بيغيّر شكل على الشاشة** → **لقطة قبل/بعد إلزامية**

## B-2 · توحيد أحمر الاستيراد — الستة ملفات

**انت اللي لقيتها. اقفلها.**

```
app/import/calculator.tsx:22   app/import/order/[id].tsx:32
app/import/auctions.tsx:22     app/import/index.tsx:34
app/import/documents.tsx:21    components/import/OrderDocuments.tsx:34
        const RED = "#E53935";     ← ΔE ≈ 19 عن اللوجو
```

**+ لوحة قوس قزح** في `order/[id].tsx:41-46` (برتقالي · أزرق · بنفسجي · أخضر) — بتكسر قاعدة `sectionTheme.ts:9-14` المقفولة نصًا.

**اقتراحك مقبول:** التقدّم يتقال **بالامتلاء مش باللون** — مسار رمادي · المكتمل بأحمر الهوية · الحالي أفتح · الجاي رمادي · **الأخضر للتسليم بس**.

**المترتب:**
- ⚠️ **بيغيّر ألوان على الشاشة** → لقطة قبل/بعد لكل شاشة استيراد
- ✅ بيقفل خامس درجة حمرا في التطبيق

## B-3 · توسيع حارس النزاهة لـ `app/import`

⚠️ **الحارس اتعمل النهاردة** — `tests/import-honesty-guard.test.mjs` (4 اختبارات، مربوط).
**مهمتك مش تعمله من الأول — مهمتك توسّعه** ليغطي الستة ملفات كلها، مش الهب والمزادات بس.

## B-4 · النيوترالز في ملف توكنز واحد

**دَين انت كتبته بأمانة.** السيارات بقت `VOID #090909` · `ASH #A5A5A5` · فاصل `0.06`.
إخواتها لسه `#000000` / `#8E8E93` — والمصانع كمان (اتكتبت النهاردة على النمط القديم).

**⚠️ الترتيب مهم:** اعمل ده **بعد B-1** — عشان متلمسش الهيدرات مرتين.

---

# 3) 🅐 مهام الوكيل A — الحجز والاكتشاف

## A-1 · فحص بنية الحجوزات كاملة 🔴 **الأولوية القصوى**

**أمر المالك:** «القسم ده ميني-آب متكامل وكبير، وكان محتاج بوليش وفحص».

| السطح | الملف | الحجم |
|---|---|---|
| الميني-آب | `components/search/BookingStaysApp.tsx` | **1140 سطر** |
| الهيدر | `components/search/stays/StaysHomeHeader.tsx` | 456 |
| حجوزاتي | `app/bookings.tsx` | — |
| مركز الإيجارات | `app/rentals/hub.tsx` | — |

**⚠️ فرق معماري:** `BookingStaysApp` **مستقل تمامًا** عن `SectionSearchApp` — فأي نمط بيتطبق عليه لازم يتوصّل بنفسه.

**الفحص يجاوب:** الحجز فعلًا شغال end-to-end؟ · الأسعار والتوفر من داتا حقيقية؟ · التقويم؟ · الإلغاء؟ · إيه المكرر مع العقارات؟

## A-2 · هيدر الحجز — الشرائح الثلاث
نفس نموذج المصانع + **تمرير `listHeader` بنفسه**.

## A-3 · المستطيلات الخمسة — تحديث ذكي عصري

**المالك حدّد: من الخرايط لحد البنوك والممولين.**

| # | `testID` | السطر |
|---|---|---|
| 1 | `discover-explore-map` (+`map-portals`) | 271 · 314 |
| 2 | `discover-car-import` | 383 |
| 3 | `discover-supply-portal` | 432 |
| 4 | `discover-importers-hub` | 474 |
| 5 | `discover-banks-hub` | 516 |

**⚠️ البنوك استثناء مقفول:** `BANKS_ACCENT = "#1668B5"` — **القسم الوحيد اللي بيخرج من عائلة الأحمر بقرار موثّق** (`sectionTheme.ts`). ⛔ **ممنوع تحوّله لأحمر.**

## A-4 · توحيد هوية كروت الاكتشاف
الكارت بيقول «عقارات» والشاشة بتقول **B-PROPERTIES**. أربعة من خمسة مقطوعين.
⛔ **ممنوع تلمس `home.categories`** — مستخدمة بره الاكتشاف.

---

# 4) 🔶 محال للمالك — مش لأي وكيل

| # | البند | ليه قرار مالك |
|---|---|---|
| 1 | **المطوّر/الكمبوند ككيان** — `schema/index.ts:2546` فيه `reference_developers` مزروع بأسماء حقيقية، **بلا endpoint وبلا شاشة** | منتج جديد مش إصلاح |
| 2 | **رسم المنطقة على الخريطة** — ✅ المالك وافق · متوسط المخاطرة | يحتاج تخصيص وكيل |
| 3 | **`profile.tsx` — 49 ضغطة في 4445 سطر** | إعادة تنظيم سطح كامل |
| 4 | **الأصول الميتة** — `section-hero/car.png` + `materials.png` = **1.29MB مش مستخدمة** | توصّل ولا تتشال؟ |
| 5 | **`ensureSchemaPatches`** لسه بتتنادى كل إقلاع جنب الـmigrations | سلطتين على الـschema |
| 6 | **الـ CI معملش ران ولا مرة** — `total_count: 0` | تشغيل واحد يقفل الشك |

---

# 5) 🖼️ الميديا — القرار المؤجَّل

| البند | الحالة |
|---|---|
| `section-hero/car.png` (1.1MB) · `materials.png` (199KB) | 🔴 **مش مربوطين بأي كود** — لقاها مدقّقان مستقلان |
| لوحات العقارات · الحجز · المصانع | ❌ ناقصة — عند المالك |
| `categories/*.jpg` (5 صور) | ✅ مستخدمة فعلًا في الاكتشاف والهيدرات |
| رفع ميديا المستخدم | ✅ `upload_claims` محروس في `health.ts:106` — ملكية الرفع شرط للجاهزية |

**المترتب على قرار الأصول الميتة:**
- **نوصّلها** → الهيرو يبقى صورة سينمائية · **بس ده يخالف «SVG فقط»**
- **نشيلها** → 1.29MB أقل · والهيرو يفضل متجهي · **ومطابق للمواصفة**

**توصيتي: نشيلها** — الهيرو دلوقتي `react-native-svg` خام، والمواصفة قاطعة، والصورتين نايمين من غير فايدة.

---

# 6) المونوريبو — قواعد المس

**8 مكتبات · 8 أسطح.** أي تغيير في `lib/*` **بيمس كل الأسطح**.

| الطبقة | القاعدة |
|---|---|
| `lib/taxonomy` | ⛔ **ممنوع** بدون قرار مالك — الموبايل والويب والأدمن والـAPI كلهم عليها |
| `lib/search-contract` | ⛔ ممنوع بدون مراجعة — `173 operationId` مربوطة |
| `lib/design-tokens` | ⚠️ الوكيل B بس (B-4) · **يستلزم `typecheck:website`** |
| `lib/db` | ⛔ ممنوع — يستلزم migration |
| `artifacts/banco-mobile` | ✅ نطاق الشغل |

**⚠️ أي كوميت بيلمس `lib/*` لازم يشغّل:**
```bash
pnpm run typecheck            # كل الأسطح
pnpm run typecheck:website
```

---

# 7) الدمج الناجح — البروتوكول

```
① احجز البند في القناة
② git pull --rebase origin main
③ نفّذ — كوميت صغير واحد
④ البوابات الأربع
⑤ لقطة قبل/بعد لو غيّرت شكل
⑥ ادمج على main فورًا — متسبش فرع بعيد
⑦ اكتب في القناة بالـ SHA
```

**⛔ ممنوع تسيب فرع أكتر من يوم.** التصادم النهاردة سببه فرع عاش لوحده.

## البوابات الأربع

```bash
cd artifacts/banco-mobile
pnpm run typecheck                                   # 0
pnpm run test                                        # 300 · 0 فشل
cd ../.. && node scripts/chain-integrity-gate.mjs    # 198/198
node scripts/production-confidence-check.mjs         # 20/20
```

## ضغط متعدد شديد — قبل أي تسليم يغيّر شكل

| # | الاختبار |
|---|---|
| 1 | تبديل اللغة **على نفس الشاشة** |
| 2 | RTL كامل |
| 3 | 320dp · 360 · 390 · 430 — **320 هي اللي كشفت قص العلامة** |
| 4 | خط النظام مكبّر |
| 5 | صفر نتائج · شبكة بطيئة |
| 6 | ضغط متكرر سريع — مفيش تنقّل مزدوج |

## طريقة الفحص البصري — شغالة، متضيعش وقت تكتشفها تاني

```bash
ln -sfn "$PWD/artifacts/banco-mobile/node_modules/expo" node_modules/expo
cd artifacts/banco-mobile && pnpm build:web
PORT=3111 BASE_PATH=/banco-mobile node server/serve.js
# http://localhost:3111/banco-mobile/   ← مش على الجذر
# Chromium متثبت: /opt/pw-browsers/chromium — متعملش playwright install
```

---

# 8) النشر — الحالة والمترتب

| البند | الحالة |
|---|---|
| `production-confidence-check` | ✅ **20/20** |
| `openapi` ↔ العميل | ✅ **173 operationId** متطابقة |
| migrations | ✅ **71 جدول** · صفر SQL مدمّر |
| قوالب `well-known` | ⚠️ **شايلة `REPLACE_*`** — يدوي قبل توثيق المتجر |
| **CI** | 🔴 **معملش ران ولا مرة** — كل الأرقام محلية |
| baseline قاعدة الإنتاج | 🔴 **مجهول** — يحتاج `DATABASE_URL` حي |

**⚠️ المترتب:** كل «300 اختبار أخضر» اللي بنقوله **من تشغيل محلي**. الفرق ده لازم يتقال كل مرة لحد ما CI يشتغل.

---

# 9) ⛔ المحاذير — على الاتنين

| # | الأمر |
|---|---|
| 1 | **ممنوع خلط فلاتر الأقسام** — حرّاس، ثمرة W1 |
| 2 | **ممنوع أي رقم مخترع** — العدد المجهول = خانة غايبة |
| 3 | **الحرّاس بيقروا نص الملف الخام** — حتى تعليق هيفشّل البيلد |
| 4 | **ممنوع حذف** — العناصر تتنقل، وكل `testID` يفضل |
| 5 | **ممنوع تشيل حارس عشان تعدّي** — يتحدّث بقرار مكتوب أو الكود يتصلح |
| 6 | **`slot` الافتراضي `"all"`** · **`listHeader` و`scrollY` اختياريين** |
| 7 | **ممنوع تلمس شريط البحث المتحرك** `search.tsx:167-185` — شغّال |
| 8 | **البنوك أزرق بقرار موثّق** — ممنوع تحويله لأحمر |
| 9 | **ممنوع تنضيف `audit/handoff/`** |
| 10 | **ممنوع تقول «تمام»** قبل البوابات الأربع + اللقطة |

---

**الحالة:** `main` `2a81157` · typecheck 0 · **300 اختبار** · chain 198/198 · إنتاج 20/20
**الفروع:** كلها مدموجة — **صفر شغل معلّق بره `main`**

— Claude (الوكيل الأساسي) · بصلاحية المالك
