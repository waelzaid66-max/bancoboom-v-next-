# ملف تسليم مدير الإنتاج — مشروع BANCO / B-OOM

**تاريخ الإصدار:** 2026-08-02
**المستودع:** `waelzaid66-max/bancoboomstor` — **مصدر الحقيقة الوحيد**
**الحالة:** شغل جارٍ — 7 قرارات مفتوحة، واحد منها **أمني عاجل**

> **اقرأ القسم 9 (المحظورات) قبل أن تكتب سطرًا واحدًا.**
> كل بند فيه اتعلمناه بغلطة وقعت فعلًا. تكراره يكسر البيلد أو يفقد بيانات.

---

# 1 · خريطة المستودعات — أول مصدر للضياع

المشروع اتنسخ عبر **9 مستودعات**. ده مش عرضي — كان فيه **workflows بتدفع نسخ تلقائيًا**:
`sync-aws-virgen.yml` و `sync-bancooom.yml`.

| المستودع | الحالة |
|---|---|
| **`bancoboomstor`** | ✅ **الوحيد الصحيح** — اتعمل 2026-08-01 04:23 |
| `banco-with-wael` | ❌ مهجور — **لكن تاريخه فيه مفتاح دفع مكشوف** (القسم 8) |
| `-BANCO-CA-OOM-` · `aws-virgen` · `bancooom` · `bancotoday` · `bancostormain` · `bancostormainvirgen` · `bancoo` | ❌ أسلاف متجاوَزة |

**الـ workflows اتأرشفت** في `.github/workflows-archive/` — **متعيدهاش لمكانها** أو المستودع هيتفرّع تاني خلال أسبوع.

### جرد الهجرة — بالعدّ مش بالثقة

- **2,535 → 2,547 ملف · صفر ملف ضايع**
- الملفين "الناقصين" هما الـ workflows المؤرشفة
- فحص 57 فرع في المستودع القديم: **48 مدموجين**، 8 فيهم شغل غير مدموج — **99% منه تقارير `docs(council)` مش كود**

---

# 2 · شغل الزميل (صاحب المشروع) — 15 كوميت

> شاته اختفى، **لكن شغله كامل ومسجّل في git**. ده أدق من أي تقرير مكتوب.

## 2.1 · التأسيس — `89d28d3` (08-01 04:23)

قبل ما ينقل سطر واحد عمل **تدقيق جنائي (Phase 0)**: قارن النسخة المرشحة بـ
**55 فرع + 8 نسخ ريبو على القرص**.

النتيجة المسجّلة في الكوميت حرفيًا:

> *النسخة الأساسية **مجموعة شاملة صارمة** لكل نسخة تانية: **3807 رمز مُصدَّر مقابل 3547**
> في أقرب منافس، **بصفر رمز وصفر ملف مصدري ناقص في أي مكان**.*

**نُقل:** 2,536 ملف · 402,921 سطر — متحقق `byte-identical`
**اتشال:** مفتاح `PAYMENT_CONFIG_ENCRYPTION_KEY` من `.replit`
**اتأرشف:** الـ sync workflows

## 2.2 · نظام الهجرات — `62c1dbe` · `cf3f8d0` · `e3c19cc` · `d43365c`

**المشكلة:** 71 جدول و49 enum بـ **صفر ملف migration**، وكل حاجة بـ `drizzle-kit push --force`.
و`--force` موجود عشان **يخرس تحذير فقدان البيانات** — تغيير اسم عمود بياخد بياناته في صمت.

**بنى:**
- `lib/db/src/migrate.ts` — بيطبق SQL بالترتيب، مرة واحدة لكل ملف، ويفشل مقفول
- `lib/db/src/baseline.ts` — **الحتة الذكية**: الداتابيز فيها الجداول أصلًا، فبيسجّل البصمات **من غير تنفيذ**، ويرفض يشتغل على داتابيز فاضية
- `migrations/0000_fantastic_warbird.sql` — **293 CREATE · صفر DROP**
- `migrations/0001_minor_stingray.sql`
- `lib/db/MIGRATIONS.md` — خطة التبني بالترتيب

## 2.3 · باقي شغله

| الكوميت | الشغل |
|---|---|
| `f86d1ce` | صفحات `/sign-up` و `/sign-in` — قبلها التسجيل كان **حالة جوه تبويب البروفايل** ومفيش عنوان يتفتح |
| `2f3754a` | إصلاح رفض المستخدمين الجدد في أول تعامل |
| `a7749fe` | خطة البنوك تبقى قابلة للبيع للبنوك |
| `69a6897` | إشعارات الاستيراد بلغتين + حماية الـ14 نوع |
| `5e389b3` | **إيقاف استعلام المحادثات من كل شاشة** — تحسين أداء |
| `ebe56c5` · `8d7105a` · `6e89b96` · `3aee255` | حرّاس: صندوق الرسائل · سباق viewport الخرائط · قاعدة النشر · صلاحيات الستاف |

## 2.4 · أمانته المهنية — المعيار المطلوب

كتب في كوميته بنفسه:

> *"**NOT yet verified by execution** — generate, migrate and baseline have not been run,
> because that needs an install and a database."*

**راجل بيقول إيه اللي متحققش منه.** ده اللي خلّى باقي كلامه يستاهل الثقة. **نفس المعيار مطلوب من أي حد بعده.**

---

# 3 · شغلي أنا — 11 كوميت

## 3.1 · الملفات المعدّلة بالكامل

| الملف | التغيير |
|---|---|
| `components/search/car/CarsHomeHeader.tsx` | إعادة بناء كاملة + `slot` + اللون + نوع الأيقونة |
| `components/search/car/VehicleGlyph.tsx` | **جديد** — 21 أيقونة SVG · `strokeWidth` 1.6→2 |
| `components/search/SearchResultsSurface.tsx` | **`listHeader`** + إصلاح `scrollEnabled` |
| `components/search/SectionSearchApp.tsx` | طيّ الفلاتر · `carScrollHeader` · توصيل `listHeader` |
| `components/search/property/PropertyHomeHeader.tsx` | إصلاح padding سالب |
| `components/search/stays/StaysHomeHeader.tsx` | إصلاح padding سالب |
| `constants/i18n.ts` | 42 مفتاح × (EN + AR) |
| `tests/car-hero-honesty-guard.test.mjs` | **جديد** — 5 اختبارات |
| `assets/images/section-hero/car.png` | **جديد** — 1216×453 |
| `assets/images/section-hero/materials.png` | **جديد** — 488×291 |
| `scripts/post-merge.sh` · `docker-compose.coolify.yml` · `docker-compose.prod.yml` · `deploy/aws/scripts/db-migrate.sh` | `push --force` → `run migrate` |
| `pnpm-workspace.yaml` | حسم `dtrace-provider` |

## 3.2 · الاكتشاف الجذري — سبب "الهيدر ثابت"

`SearchResultsSurface.tsx` بيرندر `FlatList` **من غير `ListHeaderComponent` خالص**
(بحث في كل `components/search/` رجع **صفر**). والهيدر والشرائح والنتائج **إخوة** في
`SectionSearchApp.tsx:1338`.

**النتيجة:** الهيدر بياخد شريحة ثابتة من الشاشة للأبد. **مستحيل يختفي مع السكرول** بدون تغيير البنية.

## 3.3 · النتيجة المقيسة من الـ DOM الحقيقي

| | قبل | بعد |
|---|---|---|
| الكروم المثبّت (السيارات) | **465dp** | **136dp** |
| أول بطاقة | 511dp من 932 (**55% من الشاشة**) | — |

## 3.4 · اللون — بالقياس مش بالرأي

سحبت اللون السائد من `assets/images/banco-logo.png` = **`#CF1626`**

| المرشح | البُعد لكل قناة |
|---|---|
| **`#CC1E24`** (`sectionAccent.car`) | **3 · 8 · 2** |
| `#E60012` (المواصفة المكتوبة) | 23 · 22 · 38 |

اتربط بـ `sectionAccent("car")` بدل رقم مكتوب. التطبيق نزل من **5 لـ 4** درجات حمرا.

## 3.5 · إصلاحات نشر — خطر فقدان بيانات

**4 مسارات** كانت بتشغّل `push --force` على قواعد فيها بيانات. الرابع
(`deploy/aws/scripts/db-migrate.sh`) **مش في جدول الزميل** — لقيته بمسح كل ملفات التشغيل.

**سِبت بقصد:** `ci.yml` · `deploy.yml` · `replit-dev-setup.sh` — قواعد مؤقتة، و`MIGRATIONS.md` بيسمح.

---

# 4 · طلبات المالك — المواصفات المستلمة

## 4.1 · مواصفة هيدر السيارات (2026-08-01)

خلفية `#050505` · سطح `#111111` · أكسنت `#E60012` · هيرو 240-260px ·
بحث 56px نصف قطر 20 · **21 فئة مركبات** · شريط إحصائيات ·
**الفلاتر ممنوعة داخل الهيرو**.

## 4.2 · مواصفة الهيدر الديناميكي (2026-08-02) — الأحدث

خلفية `#090909` · ثانوي `#121212` · كروت `#181818` · أحمر `#E60012` · أكسنت `#FF2B2B`
مفرود **94px** · منكمش **60px** · نصف قطر سفلي 20 · أزرار 48×48
بحث **54px نصف قطر 28** · لوجو **100%→82%** · هيرو **يتلاشى** · **220ms ease-out بدون bounce**
**SVG فقط — ممنوع PNG**

📄 موثّقة كاملة: `audit/handoff/DYNAMIC-HEADER-SPEC-AR.md`

## 4.3 · قواعد ملزمة من المالك

1. **ممنوع خلط فلاتر أي قسم بأي قسم آخر** — *"دة كان الهدف من الفصل"*
2. **ممنوع أي بيانات وهمية إطلاقًا**
3. **ممنوع كسر أي شيء شغال · ممنوع الحذف**
4. **كل قسم = ميني-أب مستقل متصل بالمنصة**

## 4.4 · الصور المستلمة

| الصورة | المحتوى |
|---|---|
| IMG_8300/8303 | الوضع الحالي للسيارات — 5 صفوف فلاتر متعرّجة |
| IMG_8400 | B-CORE — الهيدر 28% من الشاشة |
| IMG_8401/8403 | B-PROPERTIES — صفّين فلاتر + شريحة غير متسقة |
| IMG_8404 | مصانع وأراضي — **بلا هوية** |
| IMG_8405 | B-OOM STAY — **الشعار إنجليزي في تطبيق عربي** |
| IMG_8408 + رندر عريض | **التصميم المستهدف للسيارات** → استُخرج منه `car.png` |
| IMG_8409 + FullSizeRender | B-PROPERTIES — **مائلة وفيها انعكاس، غير صالحة كأصل** |
| FullSizeRender (B-CORE) | → استُخرج منه `materials.png` |

---

# 5 · تدقيق الخمس هيدرز

| # | القسم | الملف | الارتفاع | العلّة |
|---|---|---|---|---|
| ① | Property | `search/property/PropertyHomeHeader.tsx` | ~309dp | 4 أيقونات بلا تسميات · "POWERED BY" سطر لوحده · **صفّين فلاتر** · شريحة "الكل" أطول من جيرانها |
| ② | Cars | `search/car/CarsHomeHeader.tsx` | **465→136** | ✅ اتصلحت |
| ③ | Materials | `search/materials/MaterialsHomeHeader.tsx` | ~180 + 105 | محاذاة متعرّجة |
| ④ | Factories | *(لا يوجد)* | — | 🔴 **بلا هوية** — بيقع على الهيدر العام |
| ⑤ | Stays | `search/stays/StaysHomeHeader.tsx` | ~300dp | **شعار إنجليزي** · صفّين فلاتر |

**سبب التناثر:** `SectionSearchApp.tsx` → `chipStrip` فيه `flexWrap: "wrap"`

## 🔴 العقبة البنيوية — **أهم بند فني مفتوح**

السيارات نجحت لأن **الوردمارك جوه الشريط العلوي**.
في العقارات والمواد والحجز الوردمارك في **كتلة منفصلة تحت** الشريط.

**لو اتطبق نفس التقسيم حرفيًا → الوردمارك هينزل تحت شرائح الفلاتر = شكل غلط.**

**الحل المقترح:** نقل الوردمارك المصغّر لجوه الشريط العلوي في التلاتة (زي السيارات).
**محتاج قرار.**

---

# 6 · عيوب مرصودة بـ file:line

## 6.1 · 🔴 باج بيمنع النشر فعليًا

`rental_term` معلّم **"اختياري"** لكنه **بيمنع الإرسال**:
`listingCreateTaxonomy.ts:274` بلا `required` · `:384-386` بيضيفه للمطلوب لما `offer_type === "rent"`

→ **مستخدم بينشر إعلان إيجار ومش عارف ليه مش راضي يتبعت.**

## 6.2 · حقول متناقضة

| المشكلة | الدليل |
|---|---|
| سيارات: `year`/`mileage`/`fuel_type` "مطلوب" لكن مخفيين جوه "تفاصيل أكثر" | `:250-253` مقابل `:329` |
| `raw_materials.industry` نفس المشكلة | `:303` مقابل `:389-391` |
| `SPEC_LABELS` ناقصه 8 مفاتيح → **العربية تعرض إنجليزي** | `listingSpecs.ts:14-62` |
| `FINISHING_TYPES`=`"finished"` و`VALUE_LABELS`=`fully_finished` | `taxonomy:144` / `listingSpecs.ts:66` |

## 6.3 · أزرار الدول مفرودة

`MarketCountryPicker.tsx` عنده **3 مستويات** — `trigger` 180px · `triggerCompact` 160px · `triggerMicro` 88px

- `triggerMicro` مستخدم في **مكان واحد بس** (`PropertyHomeHeader:316`)
- الـ prop `compact` **ميت** — متعرّف `:286` ومش متمرر في أي مكان
- **أسوأ موضع:** `search.tsx:775` — صف كامل لوحده = ~42dp ضايعة
- **إنشاء الإعلان:** السوق + العملة + المنشأ = **3 حقول مستقلة** ≈ 150dp

## 6.4 · استيراد السيارات بلا هوية

- بيخترع `RED = "#E53935"` (`app/import/index.tsx:32`) — مش أي أحمر تاني في التطبيق
- **ولا ملف بيستورد** `lib/sectionTheme.ts`
- **ولا ملف بيحمّل** `boom-logo.png` أو `banco-logo.png`
- `order/[id].tsx:41-46` **ألوان قوس قزح** بتكسر قاعدة عائلة الأحمر (`sectionTheme.ts:9-14`)
- ادعاءات غير مدعومة: "Integration-ready" بلا تكامل · أرقام `"8+"` و`"21"` ثابتة

---

# 7 · 🚨 البند الأمني — الأخطر على الإطلاق

**مفتاح تشفير مدفوعات AES-256 حقيقي لسه في تاريخ `banco-with-wael` — والمستودع public.**

- موجود من كوميت *"Published your App"* → **كل نسخة اتعملت شايلاه**
- الأخطر: `[userenv]` بياخد أولوية على الـ Secrets المشفّرة → **المفتاح المكشوف كان بيلغي الآمن فعليًا**
- اتشال من `bancoboomstor` — **لكن التاريخ القديم شايله**

## المطلوب (بيد المالك حصريًا)

1. **تدوير المفتاح**
2. **إعادة حفظ كل إعدادات الدفع** — البيانات المشفّرة مربوطة بالمفتاح القديم

**ده أخطر من أي هيدر. الأولوية القصوى.**

---

# 8 · خطوة يدوية تُوقف النشر

بعد تحويل مسارات النشر لـ `migrate`، **كل داتابيز موجودة لازم تتختم مرة واحدة**:

```bash
DATABASE_URL=... pnpm --filter @workspace/db run baseline
```

من غيرها `migrate` هيفشل على أول `CREATE TABLE`.
**الفشل آمن** (مش بيكتب حاجة) **بس بيوقف النشر**. داتابيز فاضية مش محتاجة ختم.

---

# 9 · ⛔ المحظورات — اتعلمناها بأخطاء وقعت فعلًا

## 9.1 · محظورات الكود

**① ممنوع خلط فلاتر الأقسام.** كل قسم يملك نسخته من `useSearchMiniApp`
(`SectionSearchApp.tsx:196`). محمي بحرّاس: `no category melt` · `cars-force` ·
`lockCategory` · `Discover→Search melt bridge`.

**② ممنوع أي بيانات وهمية.** محمي بـ `tests/car-hero-honesty-guard.test.mjs`.
**متحقق إنه بيمسك:** حقنت `"1.2M+"` والاختبار فشل، شلتها ونجح.

**③ ممنوع الحذف.** الشرائح تتنقل مش تتشال. كل `testID` يفضل.

**④ ⚠️ ممنوع كتابة اسم testID محظور حتى في تعليق.**
الحرّاس بيعملوا **grep على النص الخام**. كتبت `cars-market-beside-banco` في تعليق
توثيقي والاختبار فشل. **صِف الاسم ولا تكتبه.**

**⑤ ممنوع `onOpenMap={() => {}}`.** الحارس مسكها. أي handler فاضي = **كنترول ميت**.

**⑥ ممنوع `push --force` على أي داتابيز فيها بيانات.** الأربع مسارات اتقفلت — **متفتحهاش**.

**⑦ ممنوع إعادة sync workflows** من الأرشيف — هي سبب الـ9 نسخ.

## 9.2 · محظورات البيئة

**⑧ ممنوع `pkill -f "expo"`** — بيضرب النسخة الجديدة مع القديمة. استخدم PID محدد.

**⑨ `tsc` العام في الحاوية 6.0.2 والمشروع على 5.9.3.** استخدم المحلي وإلا `TS5112` وهمي.

**⑩ لازم `npx tsc --build lib/*` قبل typecheck الموبايل** وإلا أخطاء `TS6305` وهمية.

**⑪ `pg_trgm` لازم قبل `push`:** `CREATE EXTENSION IF NOT EXISTS pg_trgm;`

**⑫ السكريبت اسمه `push-force` مش `push -- --force`.**

## 9.3 · ⚠️ حد البيئة — أخطر سوء فهم محتمل

**التطبيق لا يبعث أي طلب بحث في بيئة التطوير** (صفر نداء API — متحقق بمراقبة الشبكة).
السبب: مفيش Clerk حقيقي → التطبيق يرندر signed-out.
**النتيجة:** الكروت تفضل رمادية والـ overlay يغطي الهيرو.

**معناه:**
- ✅ قياس الأرقام والمقاسات **موثوق**
- ❌ الحكم البصري النهائي **مستحيل هنا** — محتاج جهاز حقيقي أو مفتاح Clerk تجريبي

**لا تقبل من أي حد لقطة شاشة من البيئة دي كدليل على الشكل النهائي.**

---

# 10 · بيئة التشغيل — الوصفة المتحقق منها

```bash
service postgresql start
sudo -u postgres psql -c "ALTER USER postgres PASSWORD 'postgres';"
sudo -u postgres createdb banco_test
PGPASSWORD=postgres psql -h localhost -U postgres -d banco_test \
  -c "CREATE EXTENSION IF NOT EXISTS pg_trgm;"

corepack enable && corepack prepare pnpm@11.9.0 --activate
pnpm install

export DATABASE_URL="postgresql://postgres:postgres@localhost:5432/banco_test"
pnpm --filter @workspace/db run push-force
pnpm --filter @workspace/api-server run seed:reference
pnpm --filter @workspace/api-server run seed:car-brands

PORT=3000 SESSION_SECRET="…" CLERK_SECRET_KEY="sk_test_…" \
  pnpm --filter @workspace/api-server run dev

cd artifacts/banco-mobile
env -u EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY \
    EXPO_PUBLIC_API_BASE_URL="http://localhost:3000" \
    CI=1 npx expo start --web --offline --port 8081
```

**النتيجة:** 58 إعلان (25 سيارة) · الـ API يرد `car: 25`

## بوابات التحقق — بعد **كل** مرحلة

```bash
npx tsc --build lib/*                                  # الأول
cd artifacts/banco-mobile
npx tsc --noEmit --skipLibCheck -p tsconfig.json        # لازم 0
for t in tests/*.test.mjs; do node --test "$t"; done    # 294 · 0 فشل
cd /workspace/bancoboomstor && node scripts/chain-integrity-gate.mjs  # 198/198
```

---

# 11 · أين توقفنا بالحرف

## ✅ خلص ومتحقق منه

الوصول للريبو الصح · دمج السيارات في `main` (`ca5a1ed`) · 4 مسارات نشر خطرة اتقفلت ·
آلية `listHeader` · تقسيم هيدر السيارات (**465→136dp**) · اللون بالقياس ·
سُمك الأيقونات · عيب أندرويد · لوحتين فنيتين · المواصفة موثّقة.

**كل كوميت عدّى:** `typecheck 0` · **294 اختبار** · `chain-integrity 198/198`

## ⏸️ آخر شغل — لم يبدأ

**الهيدر المتحرك بـ Reanimated** حسب `DYNAMIC-HEADER-SPEC-AR.md`.

**كل المكتبات مثبتة بالفعل:** `reanimated 4.1.1` · `gesture-handler 2.28` ·
`svg 15.12.1` · `safe-area-context 5.6` · `expo-blur 15.0.8` · `expo-router 6.0.24`
**مفيش حاجة محتاجة تتثبت.**

الآلية المبنية (`listHeader`) هي **أساس** الأنيميشن مش بديله.

## 🔓 القرارات المفتوحة السبعة

| # | البند | يوقف ماذا |
|---|---|---|
| 1 | 🚨 **تدوير مفتاح الدفع** | **أمن الإنتاج** — بيد المالك حصريًا |
| 2 | الأحمر: `#CC1E24` (قياس) أم `#E60012` (مواصفة)؟ | توحيد ألوان الاستيراد |
| 3 | «NO PNG» للأيقونات فقط أم الشعارات كمان؟ | لو الشعارات → محتاج `.svg` من المصمم |
| 4 | رندر **مسطح** للعقارات | لوحة B-PROPERTIES |
| 5 | نقل الوردمارك لجوه الشريط العلوي في 3 هيدرز؟ | يفك العقبة البنيوية |
| 6 | أرقام الإحصائيات | مالهاش مصدر في الـ API |
| 7 | باج `rental_term` | 🔴 نشر إعلانات الإيجار |

---

# 12 · الترتيب المقترح للتنفيذ

| # | البند | لماذا الآن |
|---|---|---|
| 1 | تدوير مفتاح الدفع | أمني — لا ينتظر |
| 2 | ختم قواعد البيانات (`baseline`) | يوقف النشر بدونها |
| 3 | باج `rental_term` | يمنع نشر فعلي |
| 4 | حسم القرارات 2·3·4·5 | تفتح باقي الشغل |
| 5 | الهيدر المتحرك للسيارات | النمط المرجعي |
| 6 | العقارات ← المواد ← الحجز ← المصانع | واحد واحد + بوابة بصرية |
| 7 | ضغط أزرار الدول | مكسب مساحة |
| 8 | بوليش الاستيراد | توحيد الهوية |

---

## ملفات مرجعية في المستودع

| الملف | المحتوى |
|---|---|
| `audit/handoff/PRODUCTION-MANAGER-BRIEF-AR.md` | **هذا الملف** |
| `audit/handoff/SESSION-HANDOFF-2026-08-02-AR.md` | سجل الجلسة التفصيلي |
| `audit/handoff/DYNAMIC-HEADER-SPEC-AR.md` | مواصفة الهيدر + التعارضات الثلاثة |
| `audit/handoff/BOOM-CAR-HERO-HEADER-HANDOFF.md` | تسليم هيدر السيارات |
| `lib/db/MIGRATIONS.md` | خطة الهجرات (شغل الزميل) |

**البرانش الحالي:** `claude/headers-dynamic-polish` — 7 كوميتات فوق `main`
