# مواصفة الهيدر الديناميكي — المرجع الرسمي

> مصدرها: بريف المالك (2026-08-02). ده المرجع الوحيد لأي هيدر قسم من دلوقتي.
> أي قرار بيخالفها لازم يتكتب هنا بسببه، مش يتاخد في الكود بصمت.

---

## 1. الجاهزية التقنية — كلها متحققة ✅

المواصفة بتطلب ست مكتبات، وكلها مثبتة فعلًا بإصدارات مناسبة:

| المكتبة | الإصدار |
|---|---|
| `react-native-reanimated` | ~4.1.1 |
| `react-native-gesture-handler` | ~2.28.0 |
| `react-native-svg` | 15.12.1 |
| `react-native-safe-area-context` | ~5.6.0 |
| `expo-blur` | ~15.0.8 |
| `expo-router` | ~6.0.24 |

**مفيش أي حاجة محتاجة تتثبت.** الانهيار المتحرك (collapse) قابل للتنفيذ كامل.

---

## 2. نظام الألوان المطلوب

```
الخلفية      #090909
الثانوي      #121212
الكروت       #181818
الأحمر       #E60012
الأكسنت      #FF2B2B
نص أساسي     #FFFFFF
نص ثانوي     #A5A5A5
فاصل         rgba(255,255,255,0.06)
توهج         rgba(230,0,18,0.18)
```

## 3. المقاسات

| البند | القيمة |
|---|---|
| الهيدر مفرود | 94px |
| الهيدر منكمش | 60px |
| نصف قطر سفلي | 20px |
| هامش أفقي | 16px |
| فجوة | 12px |
| أزرار دائرية | 48×48 (هدف لمس 48dp) |
| شريط البحث | ارتفاع 54 · نصف قطر 28 |
| نظام المسافات | 8dp |

## 4. السلوك الديناميكي

مع السكرول: الهيدر ينكمش · الخلفية تتموّه خفيف · **اللوجو 100% ← 82%** · **الهيرو يتلاشى 100% ← 0%** · شريط البحث يلزق · الظل يزيد.

**الحركة:** 220ms · `ease-out` · **بدون bounce ولا spring** · 60fps.

## 5. البنية

```
HeaderContainer
 └ SafeArea
    ├ TopNavigation  (MenuButton · Logo · NotificationButton · ProfileButton)
    ├ HeroSection    (VehicleArtwork)
    └ StickyContainer (SearchBar)
```

---

# ⚠️ ثلاث تعارضات لازم تتحسم قبل التنفيذ

مكتوبة هنا عشان محدش يبني على تناقض ويكتشفه بعد ما يخلص.

## تعارض ١ — الأحمر: `#E60012` أم `#CC1E24`؟

- **المواصفة** بتقول `#E60012`.
- **لكن** المالك قال قبلها: *"اللون لازم يكون قريب من الهوية للتطبيق"*.
- **والقياس الفعلي** لأصل العلامة `assets/images/banco-logo.png` طلع اللون السائد **`#CF1626`**:

| المرشح | البُعد لكل قناة عن الشعار |
|---|---|
| `#CC1E24` (`sectionAccent.car`) | **3 · 8 · 2** |
| `#E60012` (المواصفة) | 23 · 22 · 38 |

الكود حاليًا على `#CC1E24` (مربوط بـ `sectionAccent` مش رقم مكتوب).

**القرار المطلوب:** نمشي على القياس (`#CC1E24`) ولا على الرقم المكتوب في المواصفة (`#E60012`)؟
مينفعش الاتنين — التطبيق فيه أصلًا أربع درجات حمرا متعايشة.

## تعارض ٢ — «ONLY SVG · NO PNG» مقابل الأصول الحالية

المواصفة قاطعة: *"NO PNG. NO JPG. NO raster icons."*

**الأيقونات متوافقة بالفعل ✅** — `components/icons.tsx` بيرندر عبر `lucide-react-native` (SVG)، و`VehicleGlyph.tsx` مرسوم بـ `react-native-svg` مباشرة.

**اللي مش متوافق:**
- `banco-logo.png` و `boom-logo.png` — شعارات raster (10 ملفات PNG في `assets/images`)
- `section-hero/car.png` و `materials.png` — لوحات الهيرو اللي استخرجتها من رندرات المالك

**الواقع:** الصورة السينمائية (سوبركار · يخت · طيارة · هليكوبتر) **مستحيل تكون SVG** — دي رندر ضوئي واقعي، مش رسم متجهي.

**القرار المطلوب:** القاعدة تتفهم إنها **للأيقونات فقط** (وساعتها إحنا ملتزمين)، ولا مطلوب فعلًا شعارات SVG كمان؟
لو الشعارات SVG مطلوبة، محتاج ملفات `.svg` أصلية من المصمم.

## تعارض ٣ — انهيار متحرك مقابل القرار السابق

المواصفة بتطلب **انهيار بـ Reanimated** (لوجو يتصغّر، هيرو يتلاشى، تموّه، ظل).

قبل كده اختار المالك **"الأأمن من الحلين"**، فنفّذت الحل غير المتحرك: الهيرو راح جوه `ListHeaderComponent` فبيمشي مع السكرول بدون أنيميشن. ده شغال ومتحقق منه دلوقتي (المثبّت نزل من **465dp لـ 136dp**).

**المواصفة دي بتلغي الاختيار ده.** المسار الجديد يحتاج:
- `Animated.FlatList` + `useAnimatedScrollHandler`
- الهيدر يرجع بره القائمة بموضع `absolute`
- `interpolate` على الارتفاع/الشفافية/المقياس

**ملاحظة مهمة:** الآلية اللي بنيتها (`listHeader` في `SearchResultsSurface`) **متضيعش** — هي اللي بتخلي المحتوى يمر تحت الهيدر المثبّت. الأنيميشن بيتبني فوقها مش بدلها.

---

# قواعد ملزمة (من المالك، مش من المواصفة)

1. **عزل الأقسام مقدّس.** ممنوع تخلط فلاتر أي قسم بأي قسم تاني. كل قسم بيملك نسخته من `useSearchMiniApp` — محمي بحرّاس (`no category melt` · `cars-force` · `lockCategory`).
2. **ممنوع أي بيانات وهمية.** أرقام الموك-أب (`1.2M+ مركبة` · `45,840 عقار` · `2,450 مصنع`) مالهاش مصدر في الـ API. محمي بـ `tests/car-hero-honesty-guard.test.mjs` اللي بيفشل البيلد لو رجعت.
3. **ممنوع كسر أي شيء شغال، وممنوع الحذف.**
4. بعد كل مرحلة: `typecheck 0` · اختبارات الموبايل (294) · `chain-integrity` (198/198).

---

# حالة الأصول

| القسم | اللوحة | الحالة |
|---|---|---|
| السيارات | `assets/images/section-hero/car.png` (1216×453) | ✅ جاهزة — منظّفة من كل نص مدموج |
| المواد B-CORE | `assets/images/section-hero/materials.png` (488×291) | ✅ جاهزة |
| العقارات | — | ❌ **ناقصة** — الرندر الوحيد المتاح صورة موبايل مائلة فيها انعكاس زجاج، القص منها هيدخّل الميلان والانعكاس جوه التطبيق |
| الحجز / المصانع | — | لسه المالك بيجهّزهم |
