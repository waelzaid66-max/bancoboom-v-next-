# Supervision pass — `canonical @ 3951c72`

Integrity supervision: every check below exists to detect manipulation, weakening, or silent loss. Executed **2026-08-21 06:30 UTC**.

**Result: no manipulation. Protection increased. But the CI fix does not do what it may appear to do — §3.**

---

## 1 · Integrity checks — all pass

| Check | Result |
|---|---|
| Any previous canonical head dropped from history? | ✅ **None** — `e4b8f29`, `f45c32c`, `1ccdbac`, `5f44c86`, `8051943` are all still ancestors |
| History rewritten or force-pushed? | ✅ **No** — 247 commits, append-only |
| The nanoid fix still in history? | ✅ **Yes** — `76f7f26` intact |
| Chain assertions reduced? | ✅ **No — increased 242 → 245** |
| Any gate or job removed from CI? | ✅ **No** — the workflow diff is purely additive |
| Security posture weakened? | ✅ **No** — 0 blocking, the two dated waivers unchanged |
| Test counts reduced? | ✅ **No** — confidence 26/26, render **124/124** across 17 suites |
| Working tree dirty at canonical checkout? | ✅ **Clean** |
| Tags created (would fire the deploy path)? | ✅ **0** |

## 2 · ✅ The workflow change is correct, and it was pinned immediately

`3951c72 fix(ci): validate canonical assembly pushes` changes three workflow files by **one line each**:

```diff
 on:
   push:
-    branches: [main]
+    branches: [main, canonical/vnext-assembly]
   pull_request:
   workflow_dispatch:
```

**Nothing removed.** `pull_request` and `workflow_dispatch` both retained; no job, step, or gate touched. It closes a genuine gap: canonical is the production-candidate branch, yet pushes to it produced **no CI evidence at all** — only a zero-diff PR or a manual dispatch could.

**And the batch does the thing this codebase demands: it pins its own change.** Three new chain assertions were added in the same commit:

```js
id: `P-canonical-push-ci-${name}`,          // core · website · docker
test: (s) => /push:\s*\n\s+branches:\s*\[main,\s*canonical\/vnext-assembly\]/.test(s),
why: "Every canonical production-candidate push must create executable CI
      evidence without a zero-diff PR or manual dispatch",
```

**Verified: 245/245 passes** — the count rose by exactly three. Nobody can now quietly remove `canonical/vnext-assembly` from the triggers; the gate would fail.

This is the correct pattern: change the control, then pin the change. It is the fourth consecutive batch to meet the house standard.

## 3 · 🔴 The CI fix is correct — and it does **not** restore CI

**This distinction matters, because the batch could reasonably be read as having fixed CI. It has not.**

The trigger now works. A push to canonical **did** create a run for the first time:

```
run 32454086556 · event: push · sha 3951c729
created 06:21:45Z → updated 06:21:50Z          ← 5 seconds
jobs: 0
```

**Zero jobs. Five seconds.** The run is created and dies before any job materialises.

**The platform blocker is now proven across all three trigger types:**

| Trigger | Who dispatched | Result |
|---|---|---|
| `workflow_dispatch` | **me**, on canonical | 7 jobs, all zero steps, 7 seconds |
| `pull_request` | the manager | 7 jobs, all zero steps, 3 seconds |
| `push` | the manager | **0 jobs**, 5 seconds |

Three trigger types, two different people, canonical and feature branches alike — **identical failure.** No configuration in this repository can produce that, and no further configuration change will fix it.

**What the fix did achieve, and it is worth keeping:** the trigger gap was real and is now closed *and guarded*. The moment Actions executes again, canonical pushes will produce evidence automatically. **The work is not wasted — it is simply not the cure.**

**Correction to any impression otherwise: CI remains the top blocker, unchanged.** The cause is account- or platform-level. As stated previously, the fastest discriminator is the **annotation banner on the run page in the GitHub web UI**, which the REST endpoints I can reach do not expose.

## 4 · Branch state

| Branch | State |
|---|---|
| `recovery/coworker-maps-5f44c865-20260821` | **fully merged**, 0 unique commits — nothing pending |
| `fix/recent-search-chrome-20260821` | open PR #5, unmerged |
| `fix/maps-tile-failure-state-v2-20260821` | open PR #4 — **superseded**: the maps work landed via `5f44c86` |
| `ci/final-rc-*` ×2 | open PRs #2, #3 — both target stale heads, both died at the platform |
| `fix/nanoid-override`, `maint/safe-batch-01`, `fix/sot-lock-vnext-only` | merged or superseded — retire |

**Note on PR #4:** the tile-failure implementation on canonical (`5f44c86`) uses Leaflet's native `tileerror` on a layer built from the `OSM_TILES` constant. The PR branch uses a wrapper module with a hardcoded host. **The landed version is the better one** — merging the PR now would regress it. Close PR #4 as superseded rather than merging.

## 5 · Cumulative — eight items closed, none weakened

**C-5 · I-6 · H-1 · L-1 · C-3/G-1 · C-4 · SOT lock · canonical CI triggers**

Across all eight: no guard weakened, no frozen surface disturbed, no orphaned test, no history rewritten. Chain assertions **242 → 245**. Render **120 → 124** across **16 → 17** suites. Every batch carried a static guard, and the last four also carried a real render mount or a new chain assertion.

**Supervision verdict: the agents are working to standard. No manipulation detected in any batch to date.**

## 6 · What still blocks production

**🔴 CI cannot execute** — proven across three trigger types. Above every product gap.
**⏰ 19 days** — the 2026-09-09 waiver; `latest` still `2.0.2`, upstream has not shipped.
**🟠** Discover ×4 · H-3 (must be **built**) · G-2 Clerk host allowlist · observability seam unguarded.
**🟡** C-1 tags · C-2 tile procurement · M-4 `enterprise` + `company` · H-2 needs a `pk_live_` build.
**🟢** CH-1/2/3 + the stale Maps document naming four shipped tools as missing.
**⚫** No native render · no real-browser WebView render · no live provider journey · no deployment rehearsal · no full-workspace lint.

---
*Supervision executed against `3951c72`: history verified, assertions counted, workflow diff read line by line, CI failure reproduced across three trigger types. No file modified; nothing pushed to `canonical/vnext-assembly`.*
