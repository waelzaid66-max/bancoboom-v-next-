# Chair Distrust Protocol — Interconnected Systems

**Issued by:** Chief Production Architect  
**Date:** 2026-07-31  
**Binding on:** Auditor · Reliability · Idle · Chair self  

---

## Why this exists

Owner order: architecture is large, precise, interconnected. **Do not trust what you find.** Prior agent HEALTHY / FIXED stamps are hypotheses until re-verified end-to-end on the current tip SHA.

**Proved today:** Zone B stamped REL-07 HEALTHY at `router.push`. Zone C skeptic found create **dropped** `category=industrial` (MOB-C-01). Half-path “fixes” are how precision dies.

---

## Rules (non-negotiable)

1. **Producer + consumer.** Every navigation/deep-link claim must verify **both ends** (emit URL **and** receive/parse/seed).
2. **Tip SHA on every packet.** Evidence against a stale SHA is invalid for FIXED flips.
3. **No trust in own prior packets.** Chair re-skepticizes A/B/C; seats must re-skepticize peers.
4. **Blast-radius note required** before Approve: what systems share the type/enum/helper.
5. **Do not “improve”** markets/currency/search-contract/auth tombstone while fixing an unrelated deep-link.
6. **Server backstop ≠ client complete.** Document both; fix client when UX/state lies.
7. **Guards must catch the consumer** when claiming end-to-end contracts (REL-07 now includes create remap SoT).

---

## Seat precision bar (above “smart model”)

| Seat | Extra duty |
|------|------------|
| Auditor | Challenge Chair landings with **consumer** greps; reject OPEN→FIXED without dual-end evidence |
| Reliability | Before coding: list files that import the same symbol; after coding: full mobile pack + chain + confidence |
| Idle | Inventory only; if you see a wiring claim, mark UNVERIFIED until dual-end |
| Chair | Owns cross-seat defects; force-exec only with narrow blast radius + D-record |

---

## Immediate consequence

- Zone B empty-CTA HEALTHY → **amended**: healthy emit; create consumer fixed in **REL-10** (MOB-C-01/02/03/04).
- Matrix: Zone C no longer PENDING-blind — skeptic packet is SoT until Auditor peers.
- Auditor create HEALTHY @ `3a234ef` → **SUPERSEDED** (anti-pollution presentation).
- Edit request price DEFECT → **REL-11 FIXED** (omit `base_price_cash`; never send `0`).
- Zone D/E/F packets @ `3a234ef` → **HYPOTHESIS** until Wave 4b tip rebind (`69`).
